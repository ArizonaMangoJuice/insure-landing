import React from 'react';
import logo from '../../images/logo.svg';

const Header = props => {
    return (
        <header>
            <img alt='' src={logo} />
            <nav></nav>
        </header>
    )
};

export default Header;